import './ListaSuspensa.css'

export const ListaSuspensa = (props) => {
    return (
        <div>
            <label>{props.label}</label>
            <select>
                {props.itens = }
            </select>
        </div>
    )
}